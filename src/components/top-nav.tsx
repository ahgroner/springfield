import * as React from "react";
import styled from "styled-components";
import { Title } from "./text";

const Wrapper = styled.div`
  padding: 24px;
  font-size: 24px;
`;

export default () => (
  <Wrapper>
    <div>The Simpsons Visualized</div>
  </Wrapper>
);
